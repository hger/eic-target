defaultNamespace: cattle-kubewarden-system

targetCustomizations:
  - name: dynamic-downstream-targets
    clusterSelector:
      matchLabels:
        environment: eic-target

dependsOn:
  - name: eic-target-01-namespaces

helm:
  # OCI repository path including the chart name in the URL
  repo: oci://dp.apps.rancher.io/charts/suse-security-admission-controller
  chart: suse-security-admission-controller
  releaseName: suse-security-admission-controller
  namespace: cattle-kubewarden-system
  secretName: application-collection
  valuesFiles:
    - values.yaml
  waitForData: true

options:
  install:
    createNamespace: false